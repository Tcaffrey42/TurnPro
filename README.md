# TurnPro Run 5 Static Deploy Proof

Static, no React, no Vite, no src folder, no public folder.

Visible deploy marker: RUN 5 LIVE — STATIC DEPLOY PROOF.

Build command:

```bash
npm run build
```

Output directory: dist
